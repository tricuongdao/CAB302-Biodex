package com.biodex.controller.home;

import com.biodex.controller.BaseController;

/**
 * Controller for the placeholder home screen.
 *
 * <p>Deliberately empty: it exists as the reference for the page pattern — a controller in its own
 * subpackage, extending {@link BaseController}, paired with an FXML file in its own resources
 * folder and a single entry in the route registry.
 */
public class HomeController extends BaseController {
}
